saldo_atual = float(input("digite seu saldo atual"))
valor_deposito = float(input("deposito"))
valor_retirada = float(input("retirada"))

calculo = (saldo_atual + valor_deposito -valor_retirada)

print(calculo)